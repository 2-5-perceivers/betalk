name: betalk
description: \"Useful\" chatting app

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  json_annotation: ^4.6.0
  provider: ^6.0.0
  uuid: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.2.0
  flutter_lints: ^1.0.0
  json_serializable: ^6.3.1


flutter:
  uses-material-design: true
